import { CPUTensor } from '../cpuTensor';

export function repeat(
  x: CPUTensor,
  repeats: ReadonlyArray<number> | number,
  axis?: number
): CPUTensor {
  // TODO: implement
  throw new Error();
}
