// Auto-generated by tools/compile_webgpu_shader.js. Do not edit.

export const webgpuShaders: { [index: string]: string } = {
  "binary_add_float32_0": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {
    let lhs = array_lhs[0];
    let rhs = array_rhs[0];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_float32_1": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  lhsStride0: u32,
  rhsStride0: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let lhsStride0 = metaBuffer.lhsStride0;
  let rhsStride0 = metaBuffer.rhsStride0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0];
    let rhs = array_rhs[dim0 * rhsStride0];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_float32_2": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  rhsStride0: u32,
  rhsStride1: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_float32_3": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_float32_4": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_float32_5": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_float32_6": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_float32_7": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  outShape6: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  lhsStride6: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
  rhsStride6: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let outShape6 = metaBuffer.outShape6;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let lhsStride6 = metaBuffer.lhsStride6;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  let rhsStride6 = metaBuffer.rhsStride6;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim6 = dec_tmp % outShape6;
    dec_tmp = dec_tmp / outShape6;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5+dim6 * lhsStride6];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5+dim6 * rhsStride6];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_int32_0": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {
    let lhs = array_lhs[0];
    let rhs = array_rhs[0];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_int32_1": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  lhsStride0: u32,
  rhsStride0: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let lhsStride0 = metaBuffer.lhsStride0;
  let rhsStride0 = metaBuffer.rhsStride0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0];
    let rhs = array_rhs[dim0 * rhsStride0];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_int32_2": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  rhsStride0: u32,
  rhsStride1: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_int32_3": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_int32_4": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_int32_5": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_int32_6": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_int32_7": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  outShape6: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  lhsStride6: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
  rhsStride6: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let outShape6 = metaBuffer.outShape6;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let lhsStride6 = metaBuffer.lhsStride6;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  let rhsStride6 = metaBuffer.rhsStride6;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim6 = dec_tmp % outShape6;
    dec_tmp = dec_tmp / outShape6;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5+dim6 * lhsStride6];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5+dim6 * rhsStride6];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_uint8_0": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {
    let lhs = array_lhs[0];
    let rhs = array_rhs[0];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_uint8_1": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  lhsStride0: u32,
  rhsStride0: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let lhsStride0 = metaBuffer.lhsStride0;
  let rhsStride0 = metaBuffer.rhsStride0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0];
    let rhs = array_rhs[dim0 * rhsStride0];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_uint8_2": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  rhsStride0: u32,
  rhsStride1: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_uint8_3": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_uint8_4": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_uint8_5": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_uint8_6": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_add_uint8_7": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  outShape6: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  lhsStride6: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
  rhsStride6: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let outShape6 = metaBuffer.outShape6;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let lhsStride6 = metaBuffer.lhsStride6;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  let rhsStride6 = metaBuffer.rhsStride6;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim6 = dec_tmp % outShape6;
    dec_tmp = dec_tmp / outShape6;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5+dim6 * lhsStride6];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5+dim6 * rhsStride6];
    array_c[i] = lhs + rhs;
  }
}
`,
  "binary_div_float32_0": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {
    let lhs = array_lhs[0];
    let rhs = array_rhs[0];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_float32_1": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  lhsStride0: u32,
  rhsStride0: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let lhsStride0 = metaBuffer.lhsStride0;
  let rhsStride0 = metaBuffer.rhsStride0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0];
    let rhs = array_rhs[dim0 * rhsStride0];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_float32_2": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  rhsStride0: u32,
  rhsStride1: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_float32_3": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_float32_4": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_float32_5": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_float32_6": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_float32_7": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  outShape6: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  lhsStride6: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
  rhsStride6: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let outShape6 = metaBuffer.outShape6;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let lhsStride6 = metaBuffer.lhsStride6;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  let rhsStride6 = metaBuffer.rhsStride6;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim6 = dec_tmp % outShape6;
    dec_tmp = dec_tmp / outShape6;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5+dim6 * lhsStride6];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5+dim6 * rhsStride6];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_int32_0": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {
    let lhs = array_lhs[0];
    let rhs = array_rhs[0];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_int32_1": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  lhsStride0: u32,
  rhsStride0: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let lhsStride0 = metaBuffer.lhsStride0;
  let rhsStride0 = metaBuffer.rhsStride0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0];
    let rhs = array_rhs[dim0 * rhsStride0];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_int32_2": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  rhsStride0: u32,
  rhsStride1: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_int32_3": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_int32_4": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_int32_5": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_int32_6": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_int32_7": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  outShape6: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  lhsStride6: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
  rhsStride6: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let outShape6 = metaBuffer.outShape6;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let lhsStride6 = metaBuffer.lhsStride6;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  let rhsStride6 = metaBuffer.rhsStride6;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim6 = dec_tmp % outShape6;
    dec_tmp = dec_tmp / outShape6;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5+dim6 * lhsStride6];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5+dim6 * rhsStride6];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_uint8_0": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {
    let lhs = array_lhs[0];
    let rhs = array_rhs[0];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_uint8_1": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  lhsStride0: u32,
  rhsStride0: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let lhsStride0 = metaBuffer.lhsStride0;
  let rhsStride0 = metaBuffer.rhsStride0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0];
    let rhs = array_rhs[dim0 * rhsStride0];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_uint8_2": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  rhsStride0: u32,
  rhsStride1: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_uint8_3": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_uint8_4": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_uint8_5": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_uint8_6": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_div_uint8_7": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  outShape6: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  lhsStride6: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
  rhsStride6: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let outShape6 = metaBuffer.outShape6;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let lhsStride6 = metaBuffer.lhsStride6;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  let rhsStride6 = metaBuffer.rhsStride6;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim6 = dec_tmp % outShape6;
    dec_tmp = dec_tmp / outShape6;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5+dim6 * lhsStride6];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5+dim6 * rhsStride6];
    array_c[i] = lhs / rhs;
  }
}
`,
  "binary_mul_float32_0": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {
    let lhs = array_lhs[0];
    let rhs = array_rhs[0];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_float32_1": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  lhsStride0: u32,
  rhsStride0: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let lhsStride0 = metaBuffer.lhsStride0;
  let rhsStride0 = metaBuffer.rhsStride0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0];
    let rhs = array_rhs[dim0 * rhsStride0];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_float32_2": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  rhsStride0: u32,
  rhsStride1: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_float32_3": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_float32_4": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_float32_5": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_float32_6": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_float32_7": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  outShape6: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  lhsStride6: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
  rhsStride6: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let outShape6 = metaBuffer.outShape6;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let lhsStride6 = metaBuffer.lhsStride6;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  let rhsStride6 = metaBuffer.rhsStride6;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim6 = dec_tmp % outShape6;
    dec_tmp = dec_tmp / outShape6;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5+dim6 * lhsStride6];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5+dim6 * rhsStride6];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_int32_0": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {
    let lhs = array_lhs[0];
    let rhs = array_rhs[0];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_int32_1": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  lhsStride0: u32,
  rhsStride0: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let lhsStride0 = metaBuffer.lhsStride0;
  let rhsStride0 = metaBuffer.rhsStride0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0];
    let rhs = array_rhs[dim0 * rhsStride0];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_int32_2": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  rhsStride0: u32,
  rhsStride1: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_int32_3": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_int32_4": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_int32_5": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_int32_6": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_int32_7": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  outShape6: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  lhsStride6: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
  rhsStride6: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let outShape6 = metaBuffer.outShape6;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let lhsStride6 = metaBuffer.lhsStride6;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  let rhsStride6 = metaBuffer.rhsStride6;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim6 = dec_tmp % outShape6;
    dec_tmp = dec_tmp / outShape6;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5+dim6 * lhsStride6];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5+dim6 * rhsStride6];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_uint8_0": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {
    let lhs = array_lhs[0];
    let rhs = array_rhs[0];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_uint8_1": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  lhsStride0: u32,
  rhsStride0: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let lhsStride0 = metaBuffer.lhsStride0;
  let rhsStride0 = metaBuffer.rhsStride0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0];
    let rhs = array_rhs[dim0 * rhsStride0];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_uint8_2": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  rhsStride0: u32,
  rhsStride1: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_uint8_3": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_uint8_4": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_uint8_5": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_uint8_6": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_mul_uint8_7": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  outShape6: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  lhsStride6: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
  rhsStride6: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let outShape6 = metaBuffer.outShape6;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let lhsStride6 = metaBuffer.lhsStride6;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  let rhsStride6 = metaBuffer.rhsStride6;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim6 = dec_tmp % outShape6;
    dec_tmp = dec_tmp / outShape6;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5+dim6 * lhsStride6];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5+dim6 * rhsStride6];
    array_c[i] = lhs * rhs;
  }
}
`,
  "binary_pow_float32_0": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {
    let lhs = array_lhs[0];
    let rhs = array_rhs[0];
    array_c[i] = pow(abs(lhs), rhs);
  }
}
`,
  "binary_pow_float32_1": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  lhsStride0: u32,
  rhsStride0: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let lhsStride0 = metaBuffer.lhsStride0;
  let rhsStride0 = metaBuffer.rhsStride0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0];
    let rhs = array_rhs[dim0 * rhsStride0];
    array_c[i] = pow(abs(lhs), rhs);
  }
}
`,
  "binary_pow_float32_2": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  rhsStride0: u32,
  rhsStride1: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1];
    array_c[i] = pow(abs(lhs), rhs);
  }
}
`,
  "binary_pow_float32_3": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2];
    array_c[i] = pow(abs(lhs), rhs);
  }
}
`,
  "binary_pow_float32_4": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3];
    array_c[i] = pow(abs(lhs), rhs);
  }
}
`,
  "binary_pow_float32_5": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4];
    array_c[i] = pow(abs(lhs), rhs);
  }
}
`,
  "binary_pow_float32_6": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5];
    array_c[i] = pow(abs(lhs), rhs);
  }
}
`,
  "binary_pow_float32_7": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  outShape6: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  lhsStride6: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
  rhsStride6: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let outShape6 = metaBuffer.outShape6;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let lhsStride6 = metaBuffer.lhsStride6;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  let rhsStride6 = metaBuffer.rhsStride6;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim6 = dec_tmp % outShape6;
    dec_tmp = dec_tmp / outShape6;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5+dim6 * lhsStride6];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5+dim6 * rhsStride6];
    array_c[i] = pow(abs(lhs), rhs);
  }
}
`,
  "binary_reluBackprop_float32_0": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {
    let lhs = array_lhs[0];
    let rhs = array_rhs[0];
    array_c[i] = select(0.0, rhs, lhs > 0.0);
  }
}
`,
  "binary_reluBackprop_float32_1": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  lhsStride0: u32,
  rhsStride0: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let lhsStride0 = metaBuffer.lhsStride0;
  let rhsStride0 = metaBuffer.rhsStride0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0];
    let rhs = array_rhs[dim0 * rhsStride0];
    array_c[i] = select(0.0, rhs, lhs > 0.0);
  }
}
`,
  "binary_reluBackprop_float32_2": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  rhsStride0: u32,
  rhsStride1: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1];
    array_c[i] = select(0.0, rhs, lhs > 0.0);
  }
}
`,
  "binary_reluBackprop_float32_3": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2];
    array_c[i] = select(0.0, rhs, lhs > 0.0);
  }
}
`,
  "binary_reluBackprop_float32_4": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3];
    array_c[i] = select(0.0, rhs, lhs > 0.0);
  }
}
`,
  "binary_reluBackprop_float32_5": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4];
    array_c[i] = select(0.0, rhs, lhs > 0.0);
  }
}
`,
  "binary_reluBackprop_float32_6": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5];
    array_c[i] = select(0.0, rhs, lhs > 0.0);
  }
}
`,
  "binary_reluBackprop_float32_7": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  outShape6: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  lhsStride6: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
  rhsStride6: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let outShape6 = metaBuffer.outShape6;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let lhsStride6 = metaBuffer.lhsStride6;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  let rhsStride6 = metaBuffer.rhsStride6;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim6 = dec_tmp % outShape6;
    dec_tmp = dec_tmp / outShape6;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5+dim6 * lhsStride6];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5+dim6 * rhsStride6];
    array_c[i] = select(0.0, rhs, lhs > 0.0);
  }
}
`,
  "binary_sigmoidBackprop_float32_0": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {
    let lhs = array_lhs[0];
    let rhs = array_rhs[0];
    array_c[i] = (1.0 - lhs) * lhs * rhs;
  }
}
`,
  "binary_sigmoidBackprop_float32_1": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  lhsStride0: u32,
  rhsStride0: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let lhsStride0 = metaBuffer.lhsStride0;
  let rhsStride0 = metaBuffer.rhsStride0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0];
    let rhs = array_rhs[dim0 * rhsStride0];
    array_c[i] = (1.0 - lhs) * lhs * rhs;
  }
}
`,
  "binary_sigmoidBackprop_float32_2": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  rhsStride0: u32,
  rhsStride1: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1];
    array_c[i] = (1.0 - lhs) * lhs * rhs;
  }
}
`,
  "binary_sigmoidBackprop_float32_3": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2];
    array_c[i] = (1.0 - lhs) * lhs * rhs;
  }
}
`,
  "binary_sigmoidBackprop_float32_4": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3];
    array_c[i] = (1.0 - lhs) * lhs * rhs;
  }
}
`,
  "binary_sigmoidBackprop_float32_5": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4];
    array_c[i] = (1.0 - lhs) * lhs * rhs;
  }
}
`,
  "binary_sigmoidBackprop_float32_6": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5];
    array_c[i] = (1.0 - lhs) * lhs * rhs;
  }
}
`,
  "binary_sigmoidBackprop_float32_7": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  outShape6: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  lhsStride6: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
  rhsStride6: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let outShape6 = metaBuffer.outShape6;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let lhsStride6 = metaBuffer.lhsStride6;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  let rhsStride6 = metaBuffer.rhsStride6;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim6 = dec_tmp % outShape6;
    dec_tmp = dec_tmp / outShape6;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5+dim6 * lhsStride6];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5+dim6 * rhsStride6];
    array_c[i] = (1.0 - lhs) * lhs * rhs;
  }
}
`,
  "binary_sub_float32_0": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {
    let lhs = array_lhs[0];
    let rhs = array_rhs[0];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_float32_1": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  lhsStride0: u32,
  rhsStride0: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let lhsStride0 = metaBuffer.lhsStride0;
  let rhsStride0 = metaBuffer.rhsStride0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0];
    let rhs = array_rhs[dim0 * rhsStride0];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_float32_2": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  rhsStride0: u32,
  rhsStride1: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_float32_3": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_float32_4": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_float32_5": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_float32_6": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_float32_7": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  outShape6: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  lhsStride6: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
  rhsStride6: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let outShape6 = metaBuffer.outShape6;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let lhsStride6 = metaBuffer.lhsStride6;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  let rhsStride6 = metaBuffer.rhsStride6;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim6 = dec_tmp % outShape6;
    dec_tmp = dec_tmp / outShape6;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5+dim6 * lhsStride6];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5+dim6 * rhsStride6];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_int32_0": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {
    let lhs = array_lhs[0];
    let rhs = array_rhs[0];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_int32_1": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  lhsStride0: u32,
  rhsStride0: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let lhsStride0 = metaBuffer.lhsStride0;
  let rhsStride0 = metaBuffer.rhsStride0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0];
    let rhs = array_rhs[dim0 * rhsStride0];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_int32_2": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  rhsStride0: u32,
  rhsStride1: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_int32_3": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_int32_4": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_int32_5": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_int32_6": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_int32_7": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<i32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  outShape6: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  lhsStride6: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
  rhsStride6: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let outShape6 = metaBuffer.outShape6;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let lhsStride6 = metaBuffer.lhsStride6;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  let rhsStride6 = metaBuffer.rhsStride6;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim6 = dec_tmp % outShape6;
    dec_tmp = dec_tmp / outShape6;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5+dim6 * lhsStride6];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5+dim6 * rhsStride6];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_uint8_0": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {
    let lhs = array_lhs[0];
    let rhs = array_rhs[0];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_uint8_1": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  lhsStride0: u32,
  rhsStride0: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let lhsStride0 = metaBuffer.lhsStride0;
  let rhsStride0 = metaBuffer.rhsStride0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0];
    let rhs = array_rhs[dim0 * rhsStride0];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_uint8_2": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  rhsStride0: u32,
  rhsStride1: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_uint8_3": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_uint8_4": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_uint8_5": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_uint8_6": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_sub_uint8_7": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<u32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<u32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  outShape6: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  lhsStride6: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
  rhsStride6: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let outShape6 = metaBuffer.outShape6;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let lhsStride6 = metaBuffer.lhsStride6;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  let rhsStride6 = metaBuffer.rhsStride6;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim6 = dec_tmp % outShape6;
    dec_tmp = dec_tmp / outShape6;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5+dim6 * lhsStride6];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5+dim6 * rhsStride6];
    array_c[i] = lhs - rhs;
  }
}
`,
  "binary_tanhBackprop_float32_0": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {
    let lhs = array_lhs[0];
    let rhs = array_rhs[0];
    array_c[i] = (1.0 - lhs * lhs) * rhs;
  }
}
`,
  "binary_tanhBackprop_float32_1": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  lhsStride0: u32,
  rhsStride0: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let lhsStride0 = metaBuffer.lhsStride0;
  let rhsStride0 = metaBuffer.rhsStride0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0];
    let rhs = array_rhs[dim0 * rhsStride0];
    array_c[i] = (1.0 - lhs * lhs) * rhs;
  }
}
`,
  "binary_tanhBackprop_float32_2": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  rhsStride0: u32,
  rhsStride1: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1];
    array_c[i] = (1.0 - lhs * lhs) * rhs;
  }
}
`,
  "binary_tanhBackprop_float32_3": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2];
    array_c[i] = (1.0 - lhs * lhs) * rhs;
  }
}
`,
  "binary_tanhBackprop_float32_4": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3];
    array_c[i] = (1.0 - lhs * lhs) * rhs;
  }
}
`,
  "binary_tanhBackprop_float32_5": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4];
    array_c[i] = (1.0 - lhs * lhs) * rhs;
  }
}
`,
  "binary_tanhBackprop_float32_6": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5];
    array_c[i] = (1.0 - lhs * lhs) * rhs;
  }
}
`,
  "binary_tanhBackprop_float32_7": `// Auto-generated by tools/generate_webgputensor_wgsl_binary_op.py

@group(0) @binding(0) var<storage, read> array_lhs: array<f32>;
@group(0) @binding(1) var<storage, read> array_rhs: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_c: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  outShape6: u32,
  lhsStride0: u32,
  lhsStride1: u32,
  lhsStride2: u32,
  lhsStride3: u32,
  lhsStride4: u32,
  lhsStride5: u32,
  lhsStride6: u32,
  rhsStride0: u32,
  rhsStride1: u32,
  rhsStride2: u32,
  rhsStride3: u32,
  rhsStride4: u32,
  rhsStride5: u32,
  rhsStride6: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let outShape6 = metaBuffer.outShape6;
  let lhsStride0 = metaBuffer.lhsStride0;
  let lhsStride1 = metaBuffer.lhsStride1;
  let lhsStride2 = metaBuffer.lhsStride2;
  let lhsStride3 = metaBuffer.lhsStride3;
  let lhsStride4 = metaBuffer.lhsStride4;
  let lhsStride5 = metaBuffer.lhsStride5;
  let lhsStride6 = metaBuffer.lhsStride6;
  let rhsStride0 = metaBuffer.rhsStride0;
  let rhsStride1 = metaBuffer.rhsStride1;
  let rhsStride2 = metaBuffer.rhsStride2;
  let rhsStride3 = metaBuffer.rhsStride3;
  let rhsStride4 = metaBuffer.rhsStride4;
  let rhsStride5 = metaBuffer.rhsStride5;
  let rhsStride6 = metaBuffer.rhsStride6;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim6 = dec_tmp % outShape6;
    dec_tmp = dec_tmp / outShape6;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let lhs = array_lhs[dim0 * lhsStride0+dim1 * lhsStride1+dim2 * lhsStride2+dim3 * lhsStride3+dim4 * lhsStride4+dim5 * lhsStride5+dim6 * lhsStride6];
    let rhs = array_rhs[dim0 * rhsStride0+dim1 * rhsStride1+dim2 * rhsStride2+dim3 * rhsStride3+dim4 * rhsStride4+dim5 * rhsStride5+dim6 * rhsStride6];
    array_c[i] = (1.0 - lhs * lhs) * rhs;
  }
}
`,
  "strided_copy_bool_0": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_x[0];
    array_y[i] = v;
  }
}
`,
  "strided_copy_bool_1": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  xStride0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let xStride0 = metaBuffer.xStride0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0];
    array_y[i] = v;
  }
}
`,
  "strided_copy_bool_2": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  xStride0: u32,
  xStride1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1];
    array_y[i] = v;
  }
}
`,
  "strided_copy_bool_3": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  xStride0: u32,
  xStride1: u32,
  xStride2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  let xStride2 = metaBuffer.xStride2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1+dim2 * xStride2];
    array_y[i] = v;
  }
}
`,
  "strided_copy_bool_4": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  xStride0: u32,
  xStride1: u32,
  xStride2: u32,
  xStride3: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  let xStride2 = metaBuffer.xStride2;
  let xStride3 = metaBuffer.xStride3;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1+dim2 * xStride2+dim3 * xStride3];
    array_y[i] = v;
  }
}
`,
  "strided_copy_bool_5": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  xStride0: u32,
  xStride1: u32,
  xStride2: u32,
  xStride3: u32,
  xStride4: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  let xStride2 = metaBuffer.xStride2;
  let xStride3 = metaBuffer.xStride3;
  let xStride4 = metaBuffer.xStride4;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1+dim2 * xStride2+dim3 * xStride3+dim4 * xStride4];
    array_y[i] = v;
  }
}
`,
  "strided_copy_bool_6": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  xStride0: u32,
  xStride1: u32,
  xStride2: u32,
  xStride3: u32,
  xStride4: u32,
  xStride5: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  let xStride2 = metaBuffer.xStride2;
  let xStride3 = metaBuffer.xStride3;
  let xStride4 = metaBuffer.xStride4;
  let xStride5 = metaBuffer.xStride5;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1+dim2 * xStride2+dim3 * xStride3+dim4 * xStride4+dim5 * xStride5];
    array_y[i] = v;
  }
}
`,
  "strided_copy_bool_7": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  outShape6: u32,
  xStride0: u32,
  xStride1: u32,
  xStride2: u32,
  xStride3: u32,
  xStride4: u32,
  xStride5: u32,
  xStride6: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let outShape6 = metaBuffer.outShape6;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  let xStride2 = metaBuffer.xStride2;
  let xStride3 = metaBuffer.xStride3;
  let xStride4 = metaBuffer.xStride4;
  let xStride5 = metaBuffer.xStride5;
  let xStride6 = metaBuffer.xStride6;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim6 = dec_tmp % outShape6;
    dec_tmp = dec_tmp / outShape6;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1+dim2 * xStride2+dim3 * xStride3+dim4 * xStride4+dim5 * xStride5+dim6 * xStride6];
    array_y[i] = v;
  }
}
`,
  "strided_copy_float32_0": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_x[0];
    array_y[i] = v;
  }
}
`,
  "strided_copy_float32_1": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  xStride0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let xStride0 = metaBuffer.xStride0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0];
    array_y[i] = v;
  }
}
`,
  "strided_copy_float32_2": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  xStride0: u32,
  xStride1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1];
    array_y[i] = v;
  }
}
`,
  "strided_copy_float32_3": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  xStride0: u32,
  xStride1: u32,
  xStride2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  let xStride2 = metaBuffer.xStride2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1+dim2 * xStride2];
    array_y[i] = v;
  }
}
`,
  "strided_copy_float32_4": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  xStride0: u32,
  xStride1: u32,
  xStride2: u32,
  xStride3: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  let xStride2 = metaBuffer.xStride2;
  let xStride3 = metaBuffer.xStride3;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1+dim2 * xStride2+dim3 * xStride3];
    array_y[i] = v;
  }
}
`,
  "strided_copy_float32_5": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  xStride0: u32,
  xStride1: u32,
  xStride2: u32,
  xStride3: u32,
  xStride4: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  let xStride2 = metaBuffer.xStride2;
  let xStride3 = metaBuffer.xStride3;
  let xStride4 = metaBuffer.xStride4;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1+dim2 * xStride2+dim3 * xStride3+dim4 * xStride4];
    array_y[i] = v;
  }
}
`,
  "strided_copy_float32_6": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  xStride0: u32,
  xStride1: u32,
  xStride2: u32,
  xStride3: u32,
  xStride4: u32,
  xStride5: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  let xStride2 = metaBuffer.xStride2;
  let xStride3 = metaBuffer.xStride3;
  let xStride4 = metaBuffer.xStride4;
  let xStride5 = metaBuffer.xStride5;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1+dim2 * xStride2+dim3 * xStride3+dim4 * xStride4+dim5 * xStride5];
    array_y[i] = v;
  }
}
`,
  "strided_copy_float32_7": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  outShape6: u32,
  xStride0: u32,
  xStride1: u32,
  xStride2: u32,
  xStride3: u32,
  xStride4: u32,
  xStride5: u32,
  xStride6: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let outShape6 = metaBuffer.outShape6;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  let xStride2 = metaBuffer.xStride2;
  let xStride3 = metaBuffer.xStride3;
  let xStride4 = metaBuffer.xStride4;
  let xStride5 = metaBuffer.xStride5;
  let xStride6 = metaBuffer.xStride6;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim6 = dec_tmp % outShape6;
    dec_tmp = dec_tmp / outShape6;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1+dim2 * xStride2+dim3 * xStride3+dim4 * xStride4+dim5 * xStride5+dim6 * xStride6];
    array_y[i] = v;
  }
}
`,
  "strided_copy_int32_0": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_x[0];
    array_y[i] = v;
  }
}
`,
  "strided_copy_int32_1": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  xStride0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let xStride0 = metaBuffer.xStride0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0];
    array_y[i] = v;
  }
}
`,
  "strided_copy_int32_2": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  xStride0: u32,
  xStride1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1];
    array_y[i] = v;
  }
}
`,
  "strided_copy_int32_3": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  xStride0: u32,
  xStride1: u32,
  xStride2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  let xStride2 = metaBuffer.xStride2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1+dim2 * xStride2];
    array_y[i] = v;
  }
}
`,
  "strided_copy_int32_4": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  xStride0: u32,
  xStride1: u32,
  xStride2: u32,
  xStride3: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  let xStride2 = metaBuffer.xStride2;
  let xStride3 = metaBuffer.xStride3;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1+dim2 * xStride2+dim3 * xStride3];
    array_y[i] = v;
  }
}
`,
  "strided_copy_int32_5": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  xStride0: u32,
  xStride1: u32,
  xStride2: u32,
  xStride3: u32,
  xStride4: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  let xStride2 = metaBuffer.xStride2;
  let xStride3 = metaBuffer.xStride3;
  let xStride4 = metaBuffer.xStride4;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1+dim2 * xStride2+dim3 * xStride3+dim4 * xStride4];
    array_y[i] = v;
  }
}
`,
  "strided_copy_int32_6": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  xStride0: u32,
  xStride1: u32,
  xStride2: u32,
  xStride3: u32,
  xStride4: u32,
  xStride5: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  let xStride2 = metaBuffer.xStride2;
  let xStride3 = metaBuffer.xStride3;
  let xStride4 = metaBuffer.xStride4;
  let xStride5 = metaBuffer.xStride5;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1+dim2 * xStride2+dim3 * xStride3+dim4 * xStride4+dim5 * xStride5];
    array_y[i] = v;
  }
}
`,
  "strided_copy_int32_7": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  outShape6: u32,
  xStride0: u32,
  xStride1: u32,
  xStride2: u32,
  xStride3: u32,
  xStride4: u32,
  xStride5: u32,
  xStride6: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let outShape6 = metaBuffer.outShape6;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  let xStride2 = metaBuffer.xStride2;
  let xStride3 = metaBuffer.xStride3;
  let xStride4 = metaBuffer.xStride4;
  let xStride5 = metaBuffer.xStride5;
  let xStride6 = metaBuffer.xStride6;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim6 = dec_tmp % outShape6;
    dec_tmp = dec_tmp / outShape6;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1+dim2 * xStride2+dim3 * xStride3+dim4 * xStride4+dim5 * xStride5+dim6 * xStride6];
    array_y[i] = v;
  }
}
`,
  "strided_copy_uint8_0": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_x[0];
    array_y[i] = v;
  }
}
`,
  "strided_copy_uint8_1": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  xStride0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let xStride0 = metaBuffer.xStride0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0];
    array_y[i] = v;
  }
}
`,
  "strided_copy_uint8_2": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  xStride0: u32,
  xStride1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1];
    array_y[i] = v;
  }
}
`,
  "strided_copy_uint8_3": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  xStride0: u32,
  xStride1: u32,
  xStride2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  let xStride2 = metaBuffer.xStride2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1+dim2 * xStride2];
    array_y[i] = v;
  }
}
`,
  "strided_copy_uint8_4": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  xStride0: u32,
  xStride1: u32,
  xStride2: u32,
  xStride3: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  let xStride2 = metaBuffer.xStride2;
  let xStride3 = metaBuffer.xStride3;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1+dim2 * xStride2+dim3 * xStride3];
    array_y[i] = v;
  }
}
`,
  "strided_copy_uint8_5": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  xStride0: u32,
  xStride1: u32,
  xStride2: u32,
  xStride3: u32,
  xStride4: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  let xStride2 = metaBuffer.xStride2;
  let xStride3 = metaBuffer.xStride3;
  let xStride4 = metaBuffer.xStride4;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1+dim2 * xStride2+dim3 * xStride3+dim4 * xStride4];
    array_y[i] = v;
  }
}
`,
  "strided_copy_uint8_6": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  xStride0: u32,
  xStride1: u32,
  xStride2: u32,
  xStride3: u32,
  xStride4: u32,
  xStride5: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  let xStride2 = metaBuffer.xStride2;
  let xStride3 = metaBuffer.xStride3;
  let xStride4 = metaBuffer.xStride4;
  let xStride5 = metaBuffer.xStride5;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1+dim2 * xStride2+dim3 * xStride3+dim4 * xStride4+dim5 * xStride5];
    array_y[i] = v;
  }
}
`,
  "strided_copy_uint8_7": `// Auto-generated by tools/generate_webgputensor_wgsl_copy_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  outShape0: u32,
  outShape1: u32,
  outShape2: u32,
  outShape3: u32,
  outShape4: u32,
  outShape5: u32,
  outShape6: u32,
  xStride0: u32,
  xStride1: u32,
  xStride2: u32,
  xStride3: u32,
  xStride4: u32,
  xStride5: u32,
  xStride6: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let outShape0 = metaBuffer.outShape0;
  let outShape1 = metaBuffer.outShape1;
  let outShape2 = metaBuffer.outShape2;
  let outShape3 = metaBuffer.outShape3;
  let outShape4 = metaBuffer.outShape4;
  let outShape5 = metaBuffer.outShape5;
  let outShape6 = metaBuffer.outShape6;
  let xStride0 = metaBuffer.xStride0;
  let xStride1 = metaBuffer.xStride1;
  let xStride2 = metaBuffer.xStride2;
  let xStride3 = metaBuffer.xStride3;
  let xStride4 = metaBuffer.xStride4;
  let xStride5 = metaBuffer.xStride5;
  let xStride6 = metaBuffer.xStride6;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let dim6 = dec_tmp % outShape6;
    dec_tmp = dec_tmp / outShape6;
    let dim5 = dec_tmp % outShape5;
    dec_tmp = dec_tmp / outShape5;
    let dim4 = dec_tmp % outShape4;
    dec_tmp = dec_tmp / outShape4;
    let dim3 = dec_tmp % outShape3;
    dec_tmp = dec_tmp / outShape3;
    let dim2 = dec_tmp % outShape2;
    dec_tmp = dec_tmp / outShape2;
    let dim1 = dec_tmp % outShape1;
    dec_tmp = dec_tmp / outShape1;
    let dim0 = dec_tmp % outShape0;

    let v = array_x[dim0 * xStride0+dim1 * xStride1+dim2 * xStride2+dim3 * xStride3+dim4 * xStride4+dim5 * xStride5+dim6 * xStride6];
    array_y[i] = v;
  }
}
`,
  "sum_bool_0_0": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {


    var v = u32(0);
    let ofs = 0u;
    v = v + array_x[ofs];
    array_y[i] = v;
  }
}
`,
  "sum_bool_0_1": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  reductionShape0: u32,
  reductionShapeStrides0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  for (var i = global_id.x; i < len; i = i + 4096u) {


    var v = u32(0);
    let ofs = 0u;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      v = v + array_x[ofs + red0 * reductionShapeStrides0];
    }
    array_y[i] = v;
  }
}
`,
  "sum_bool_0_2": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  for (var i = global_id.x; i < len; i = i + 4096u) {


    var v = u32(0);
    let ofs = 0u;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1];
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_bool_0_3": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShape2: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
  reductionShapeStrides2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShape2 = metaBuffer.reductionShape2;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  let reductionShapeStrides2 = metaBuffer.reductionShapeStrides2;
  for (var i = global_id.x; i < len; i = i + 4096u) {


    var v = u32(0);
    let ofs = 0u;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        for (var red2 = 0u; red2 < reductionShape2; red2 = red2 + 1u) {
          v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1+red2 * reductionShapeStrides2];
        }
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_bool_1_0": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShapeStrides0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0;
    v = v + array_x[ofs];
    array_y[i] = v;
  }
}
`,
  "sum_bool_1_1": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShapeStrides0: u32,
  reductionShape0: u32,
  reductionShapeStrides0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      v = v + array_x[ofs + red0 * reductionShapeStrides0];
    }
    array_y[i] = v;
  }
}
`,
  "sum_bool_1_2": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShapeStrides0: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1];
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_bool_1_3": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShapeStrides0: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShape2: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
  reductionShapeStrides2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShape2 = metaBuffer.reductionShape2;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  let reductionShapeStrides2 = metaBuffer.reductionShapeStrides2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        for (var red2 = 0u; red2 < reductionShape2; red2 = red2 + 1u) {
          v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1+red2 * reductionShapeStrides2];
        }
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_bool_2_0": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1;
    v = v + array_x[ofs];
    array_y[i] = v;
  }
}
`,
  "sum_bool_2_1": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  reductionShape0: u32,
  reductionShapeStrides0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      v = v + array_x[ofs + red0 * reductionShapeStrides0];
    }
    array_y[i] = v;
  }
}
`,
  "sum_bool_2_2": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1];
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_bool_2_3": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShape2: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
  reductionShapeStrides2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShape2 = metaBuffer.reductionShape2;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  let reductionShapeStrides2 = metaBuffer.reductionShapeStrides2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        for (var red2 = 0u; red2 < reductionShape2; red2 = red2 + 1u) {
          v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1+red2 * reductionShapeStrides2];
        }
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_bool_3_0": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShape2: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  toShapeStrides2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShape2 = metaBuffer.toShape2;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let toShapeStrides2 = metaBuffer.toShapeStrides2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim2 = dec_tmp % toShape2;
    dec_tmp = dec_tmp / toShape2;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1+todim2 * toShapeStrides2;
    v = v + array_x[ofs];
    array_y[i] = v;
  }
}
`,
  "sum_bool_3_1": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShape2: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  toShapeStrides2: u32,
  reductionShape0: u32,
  reductionShapeStrides0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShape2 = metaBuffer.toShape2;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let toShapeStrides2 = metaBuffer.toShapeStrides2;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim2 = dec_tmp % toShape2;
    dec_tmp = dec_tmp / toShape2;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1+todim2 * toShapeStrides2;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      v = v + array_x[ofs + red0 * reductionShapeStrides0];
    }
    array_y[i] = v;
  }
}
`,
  "sum_bool_3_2": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShape2: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  toShapeStrides2: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShape2 = metaBuffer.toShape2;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let toShapeStrides2 = metaBuffer.toShapeStrides2;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim2 = dec_tmp % toShape2;
    dec_tmp = dec_tmp / toShape2;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1+todim2 * toShapeStrides2;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1];
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_bool_3_3": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShape2: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  toShapeStrides2: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShape2: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
  reductionShapeStrides2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShape2 = metaBuffer.toShape2;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let toShapeStrides2 = metaBuffer.toShapeStrides2;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShape2 = metaBuffer.reductionShape2;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  let reductionShapeStrides2 = metaBuffer.reductionShapeStrides2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim2 = dec_tmp % toShape2;
    dec_tmp = dec_tmp / toShape2;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1+todim2 * toShapeStrides2;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        for (var red2 = 0u; red2 < reductionShape2; red2 = red2 + 1u) {
          v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1+red2 * reductionShapeStrides2];
        }
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_float32_0_0": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {


    var v = f32(0);
    let ofs = 0u;
    v = v + array_x[ofs];
    array_y[i] = v;
  }
}
`,
  "sum_float32_0_1": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  reductionShape0: u32,
  reductionShapeStrides0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  for (var i = global_id.x; i < len; i = i + 4096u) {


    var v = f32(0);
    let ofs = 0u;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      v = v + array_x[ofs + red0 * reductionShapeStrides0];
    }
    array_y[i] = v;
  }
}
`,
  "sum_float32_0_2": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  for (var i = global_id.x; i < len; i = i + 4096u) {


    var v = f32(0);
    let ofs = 0u;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1];
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_float32_0_3": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShape2: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
  reductionShapeStrides2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShape2 = metaBuffer.reductionShape2;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  let reductionShapeStrides2 = metaBuffer.reductionShapeStrides2;
  for (var i = global_id.x; i < len; i = i + 4096u) {


    var v = f32(0);
    let ofs = 0u;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        for (var red2 = 0u; red2 < reductionShape2; red2 = red2 + 1u) {
          v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1+red2 * reductionShapeStrides2];
        }
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_float32_1_0": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShapeStrides0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim0 = dec_tmp % toShape0;

    var v = f32(0);
    let ofs = todim0 * toShapeStrides0;
    v = v + array_x[ofs];
    array_y[i] = v;
  }
}
`,
  "sum_float32_1_1": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShapeStrides0: u32,
  reductionShape0: u32,
  reductionShapeStrides0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim0 = dec_tmp % toShape0;

    var v = f32(0);
    let ofs = todim0 * toShapeStrides0;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      v = v + array_x[ofs + red0 * reductionShapeStrides0];
    }
    array_y[i] = v;
  }
}
`,
  "sum_float32_1_2": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShapeStrides0: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim0 = dec_tmp % toShape0;

    var v = f32(0);
    let ofs = todim0 * toShapeStrides0;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1];
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_float32_1_3": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShapeStrides0: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShape2: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
  reductionShapeStrides2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShape2 = metaBuffer.reductionShape2;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  let reductionShapeStrides2 = metaBuffer.reductionShapeStrides2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim0 = dec_tmp % toShape0;

    var v = f32(0);
    let ofs = todim0 * toShapeStrides0;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        for (var red2 = 0u; red2 < reductionShape2; red2 = red2 + 1u) {
          v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1+red2 * reductionShapeStrides2];
        }
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_float32_2_0": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = f32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1;
    v = v + array_x[ofs];
    array_y[i] = v;
  }
}
`,
  "sum_float32_2_1": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  reductionShape0: u32,
  reductionShapeStrides0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = f32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      v = v + array_x[ofs + red0 * reductionShapeStrides0];
    }
    array_y[i] = v;
  }
}
`,
  "sum_float32_2_2": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = f32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1];
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_float32_2_3": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShape2: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
  reductionShapeStrides2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShape2 = metaBuffer.reductionShape2;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  let reductionShapeStrides2 = metaBuffer.reductionShapeStrides2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = f32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        for (var red2 = 0u; red2 < reductionShape2; red2 = red2 + 1u) {
          v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1+red2 * reductionShapeStrides2];
        }
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_float32_3_0": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShape2: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  toShapeStrides2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShape2 = metaBuffer.toShape2;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let toShapeStrides2 = metaBuffer.toShapeStrides2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim2 = dec_tmp % toShape2;
    dec_tmp = dec_tmp / toShape2;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = f32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1+todim2 * toShapeStrides2;
    v = v + array_x[ofs];
    array_y[i] = v;
  }
}
`,
  "sum_float32_3_1": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShape2: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  toShapeStrides2: u32,
  reductionShape0: u32,
  reductionShapeStrides0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShape2 = metaBuffer.toShape2;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let toShapeStrides2 = metaBuffer.toShapeStrides2;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim2 = dec_tmp % toShape2;
    dec_tmp = dec_tmp / toShape2;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = f32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1+todim2 * toShapeStrides2;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      v = v + array_x[ofs + red0 * reductionShapeStrides0];
    }
    array_y[i] = v;
  }
}
`,
  "sum_float32_3_2": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShape2: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  toShapeStrides2: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShape2 = metaBuffer.toShape2;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let toShapeStrides2 = metaBuffer.toShapeStrides2;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim2 = dec_tmp % toShape2;
    dec_tmp = dec_tmp / toShape2;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = f32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1+todim2 * toShapeStrides2;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1];
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_float32_3_3": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShape2: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  toShapeStrides2: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShape2: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
  reductionShapeStrides2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShape2 = metaBuffer.toShape2;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let toShapeStrides2 = metaBuffer.toShapeStrides2;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShape2 = metaBuffer.reductionShape2;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  let reductionShapeStrides2 = metaBuffer.reductionShapeStrides2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim2 = dec_tmp % toShape2;
    dec_tmp = dec_tmp / toShape2;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = f32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1+todim2 * toShapeStrides2;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        for (var red2 = 0u; red2 < reductionShape2; red2 = red2 + 1u) {
          v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1+red2 * reductionShapeStrides2];
        }
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_int32_0_0": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {


    var v = i32(0);
    let ofs = 0u;
    v = v + array_x[ofs];
    array_y[i] = v;
  }
}
`,
  "sum_int32_0_1": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  reductionShape0: u32,
  reductionShapeStrides0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  for (var i = global_id.x; i < len; i = i + 4096u) {


    var v = i32(0);
    let ofs = 0u;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      v = v + array_x[ofs + red0 * reductionShapeStrides0];
    }
    array_y[i] = v;
  }
}
`,
  "sum_int32_0_2": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  for (var i = global_id.x; i < len; i = i + 4096u) {


    var v = i32(0);
    let ofs = 0u;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1];
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_int32_0_3": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShape2: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
  reductionShapeStrides2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShape2 = metaBuffer.reductionShape2;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  let reductionShapeStrides2 = metaBuffer.reductionShapeStrides2;
  for (var i = global_id.x; i < len; i = i + 4096u) {


    var v = i32(0);
    let ofs = 0u;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        for (var red2 = 0u; red2 < reductionShape2; red2 = red2 + 1u) {
          v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1+red2 * reductionShapeStrides2];
        }
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_int32_1_0": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShapeStrides0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim0 = dec_tmp % toShape0;

    var v = i32(0);
    let ofs = todim0 * toShapeStrides0;
    v = v + array_x[ofs];
    array_y[i] = v;
  }
}
`,
  "sum_int32_1_1": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShapeStrides0: u32,
  reductionShape0: u32,
  reductionShapeStrides0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim0 = dec_tmp % toShape0;

    var v = i32(0);
    let ofs = todim0 * toShapeStrides0;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      v = v + array_x[ofs + red0 * reductionShapeStrides0];
    }
    array_y[i] = v;
  }
}
`,
  "sum_int32_1_2": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShapeStrides0: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim0 = dec_tmp % toShape0;

    var v = i32(0);
    let ofs = todim0 * toShapeStrides0;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1];
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_int32_1_3": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShapeStrides0: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShape2: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
  reductionShapeStrides2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShape2 = metaBuffer.reductionShape2;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  let reductionShapeStrides2 = metaBuffer.reductionShapeStrides2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim0 = dec_tmp % toShape0;

    var v = i32(0);
    let ofs = todim0 * toShapeStrides0;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        for (var red2 = 0u; red2 < reductionShape2; red2 = red2 + 1u) {
          v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1+red2 * reductionShapeStrides2];
        }
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_int32_2_0": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = i32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1;
    v = v + array_x[ofs];
    array_y[i] = v;
  }
}
`,
  "sum_int32_2_1": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  reductionShape0: u32,
  reductionShapeStrides0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = i32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      v = v + array_x[ofs + red0 * reductionShapeStrides0];
    }
    array_y[i] = v;
  }
}
`,
  "sum_int32_2_2": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = i32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1];
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_int32_2_3": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShape2: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
  reductionShapeStrides2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShape2 = metaBuffer.reductionShape2;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  let reductionShapeStrides2 = metaBuffer.reductionShapeStrides2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = i32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        for (var red2 = 0u; red2 < reductionShape2; red2 = red2 + 1u) {
          v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1+red2 * reductionShapeStrides2];
        }
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_int32_3_0": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShape2: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  toShapeStrides2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShape2 = metaBuffer.toShape2;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let toShapeStrides2 = metaBuffer.toShapeStrides2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim2 = dec_tmp % toShape2;
    dec_tmp = dec_tmp / toShape2;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = i32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1+todim2 * toShapeStrides2;
    v = v + array_x[ofs];
    array_y[i] = v;
  }
}
`,
  "sum_int32_3_1": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShape2: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  toShapeStrides2: u32,
  reductionShape0: u32,
  reductionShapeStrides0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShape2 = metaBuffer.toShape2;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let toShapeStrides2 = metaBuffer.toShapeStrides2;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim2 = dec_tmp % toShape2;
    dec_tmp = dec_tmp / toShape2;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = i32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1+todim2 * toShapeStrides2;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      v = v + array_x[ofs + red0 * reductionShapeStrides0];
    }
    array_y[i] = v;
  }
}
`,
  "sum_int32_3_2": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShape2: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  toShapeStrides2: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShape2 = metaBuffer.toShape2;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let toShapeStrides2 = metaBuffer.toShapeStrides2;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim2 = dec_tmp % toShape2;
    dec_tmp = dec_tmp / toShape2;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = i32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1+todim2 * toShapeStrides2;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1];
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_int32_3_3": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<i32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShape2: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  toShapeStrides2: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShape2: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
  reductionShapeStrides2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShape2 = metaBuffer.toShape2;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let toShapeStrides2 = metaBuffer.toShapeStrides2;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShape2 = metaBuffer.reductionShape2;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  let reductionShapeStrides2 = metaBuffer.reductionShapeStrides2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim2 = dec_tmp % toShape2;
    dec_tmp = dec_tmp / toShape2;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = i32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1+todim2 * toShapeStrides2;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        for (var red2 = 0u; red2 < reductionShape2; red2 = red2 + 1u) {
          v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1+red2 * reductionShapeStrides2];
        }
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_uint8_0_0": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;

  for (var i = global_id.x; i < len; i = i + 4096u) {


    var v = u32(0);
    let ofs = 0u;
    v = v + array_x[ofs];
    array_y[i] = v;
  }
}
`,
  "sum_uint8_0_1": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  reductionShape0: u32,
  reductionShapeStrides0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  for (var i = global_id.x; i < len; i = i + 4096u) {


    var v = u32(0);
    let ofs = 0u;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      v = v + array_x[ofs + red0 * reductionShapeStrides0];
    }
    array_y[i] = v;
  }
}
`,
  "sum_uint8_0_2": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  for (var i = global_id.x; i < len; i = i + 4096u) {


    var v = u32(0);
    let ofs = 0u;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1];
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_uint8_0_3": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShape2: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
  reductionShapeStrides2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShape2 = metaBuffer.reductionShape2;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  let reductionShapeStrides2 = metaBuffer.reductionShapeStrides2;
  for (var i = global_id.x; i < len; i = i + 4096u) {


    var v = u32(0);
    let ofs = 0u;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        for (var red2 = 0u; red2 < reductionShape2; red2 = red2 + 1u) {
          v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1+red2 * reductionShapeStrides2];
        }
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_uint8_1_0": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShapeStrides0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0;
    v = v + array_x[ofs];
    array_y[i] = v;
  }
}
`,
  "sum_uint8_1_1": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShapeStrides0: u32,
  reductionShape0: u32,
  reductionShapeStrides0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      v = v + array_x[ofs + red0 * reductionShapeStrides0];
    }
    array_y[i] = v;
  }
}
`,
  "sum_uint8_1_2": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShapeStrides0: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1];
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_uint8_1_3": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShapeStrides0: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShape2: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
  reductionShapeStrides2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShape2 = metaBuffer.reductionShape2;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  let reductionShapeStrides2 = metaBuffer.reductionShapeStrides2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        for (var red2 = 0u; red2 < reductionShape2; red2 = red2 + 1u) {
          v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1+red2 * reductionShapeStrides2];
        }
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_uint8_2_0": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1;
    v = v + array_x[ofs];
    array_y[i] = v;
  }
}
`,
  "sum_uint8_2_1": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  reductionShape0: u32,
  reductionShapeStrides0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      v = v + array_x[ofs + red0 * reductionShapeStrides0];
    }
    array_y[i] = v;
  }
}
`,
  "sum_uint8_2_2": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1];
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_uint8_2_3": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShape2: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
  reductionShapeStrides2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShape2 = metaBuffer.reductionShape2;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  let reductionShapeStrides2 = metaBuffer.reductionShapeStrides2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        for (var red2 = 0u; red2 < reductionShape2; red2 = red2 + 1u) {
          v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1+red2 * reductionShapeStrides2];
        }
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_uint8_3_0": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShape2: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  toShapeStrides2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShape2 = metaBuffer.toShape2;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let toShapeStrides2 = metaBuffer.toShapeStrides2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim2 = dec_tmp % toShape2;
    dec_tmp = dec_tmp / toShape2;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1+todim2 * toShapeStrides2;
    v = v + array_x[ofs];
    array_y[i] = v;
  }
}
`,
  "sum_uint8_3_1": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShape2: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  toShapeStrides2: u32,
  reductionShape0: u32,
  reductionShapeStrides0: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShape2 = metaBuffer.toShape2;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let toShapeStrides2 = metaBuffer.toShapeStrides2;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim2 = dec_tmp % toShape2;
    dec_tmp = dec_tmp / toShape2;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1+todim2 * toShapeStrides2;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      v = v + array_x[ofs + red0 * reductionShapeStrides0];
    }
    array_y[i] = v;
  }
}
`,
  "sum_uint8_3_2": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShape2: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  toShapeStrides2: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShape2 = metaBuffer.toShape2;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let toShapeStrides2 = metaBuffer.toShapeStrides2;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim2 = dec_tmp % toShape2;
    dec_tmp = dec_tmp / toShape2;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1+todim2 * toShapeStrides2;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1];
      }
    }
    array_y[i] = v;
  }
}
`,
  "sum_uint8_3_3": `// Auto-generated by tools/generate_webgputensor_wgsl_reduction_op.py

@group(0) @binding(0) var<storage, read> array_x: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<u32>;

struct MetaBuffer {
  len: u32,
  toShape0: u32,
  toShape1: u32,
  toShape2: u32,
  toShapeStrides0: u32,
  toShapeStrides1: u32,
  toShapeStrides2: u32,
  reductionShape0: u32,
  reductionShape1: u32,
  reductionShape2: u32,
  reductionShapeStrides0: u32,
  reductionShapeStrides1: u32,
  reductionShapeStrides2: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let toShape0 = metaBuffer.toShape0;
  let toShape1 = metaBuffer.toShape1;
  let toShape2 = metaBuffer.toShape2;
  let toShapeStrides0 = metaBuffer.toShapeStrides0;
  let toShapeStrides1 = metaBuffer.toShapeStrides1;
  let toShapeStrides2 = metaBuffer.toShapeStrides2;
  let reductionShape0 = metaBuffer.reductionShape0;
  let reductionShape1 = metaBuffer.reductionShape1;
  let reductionShape2 = metaBuffer.reductionShape2;
  let reductionShapeStrides0 = metaBuffer.reductionShapeStrides0;
  let reductionShapeStrides1 = metaBuffer.reductionShapeStrides1;
  let reductionShapeStrides2 = metaBuffer.reductionShapeStrides2;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    var dec_tmp = i;
    let todim2 = dec_tmp % toShape2;
    dec_tmp = dec_tmp / toShape2;
    let todim1 = dec_tmp % toShape1;
    dec_tmp = dec_tmp / toShape1;
    let todim0 = dec_tmp % toShape0;

    var v = u32(0);
    let ofs = todim0 * toShapeStrides0+todim1 * toShapeStrides1+todim2 * toShapeStrides2;
    for (var red0 = 0u; red0 < reductionShape0; red0 = red0 + 1u) {
      for (var red1 = 0u; red1 < reductionShape1; red1 = red1 + 1u) {
        for (var red2 = 0u; red2 < reductionShape2; red2 = red2 + 1u) {
          v = v + array_x[ofs + red0 * reductionShapeStrides0+red1 * reductionShapeStrides1+red2 * reductionShapeStrides2];
        }
      }
    }
    array_y[i] = v;
  }
}
`,
  "unary_abs_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = abs(v);
  }
}
`,
  "unary_abs_int32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<i32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = abs(v);
  }
}
`,
  "unary_acos_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = acos(v);
  }
}
`,
  "unary_acosh_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = acosh(v);
  }
}
`,
  "unary_asin_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = asin(v);
  }
}
`,
  "unary_asinh_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = asinh(v);
  }
}
`,
  "unary_atan_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = atan(v);
  }
}
`,
  "unary_atanh_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = atanh(v);
  }
}
`,
  "unary_copy_bool": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<u32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = v;
  }
}
`,
  "unary_copy_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = v;
  }
}
`,
  "unary_copy_int32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<i32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = v;
  }
}
`,
  "unary_copy_uint8": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<u32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<u32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = v;
  }
}
`,
  "unary_cos_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = cos(v);
  }
}
`,
  "unary_cosh_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = cosh(v);
  }
}
`,
  "unary_exp_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = exp(v);
  }
}
`,
  "unary_log_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = log(v);
  }
}
`,
  "unary_neg_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = -v;
  }
}
`,
  "unary_neg_int32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<i32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = -v;
  }
}
`,
  "unary_relu_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = select(0.0, v, v > 0.0);
  }
}
`,
  "unary_sigmoid_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = 1.0 / (1.0 + exp(-v));
  }
}
`,
  "unary_sin_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = sin(v);
  }
}
`,
  "unary_sinh_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = sinh(v);
  }
}
`,
  "unary_sqrt_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = sqrt(v);
  }
}
`,
  "unary_square_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = v * v;
  }
}
`,
  "unary_square_int32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<i32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<i32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = v * v;
  }
}
`,
  "unary_tan_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = tan(v);
  }
}
`,
  "unary_tanh_float32": `// Auto-generated by tools/generate_webgputensor_wgsl_unary_op.py

@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_b: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = array_a[i];
    array_b[i] = ((1.0 - exp(-2.0 * abs(v))) / (1.0 + exp(-2.0 * abs(v))) * sign(v));
  }
}
`,
  "gemm": `@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read> array_b: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  M: u32,
  N: u32,
  K: u32,
  strideA0: u32,
  strideA1: u32,
  strideB0: u32,
  strideB1: u32,
  alpha: f32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(8, 8, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let M = metaBuffer.M;
  let N = metaBuffer.N;
  let K = metaBuffer.K;
  let strideA0 = metaBuffer.strideA0;
  let strideA1 = metaBuffer.strideA1;
  let strideB0 = metaBuffer.strideB0;
  let strideB1 = metaBuffer.strideB1;
  let alpha = metaBuffer.alpha;
  for (var x = global_id.x; x < N; x = x + 256u) {
    for (var y = global_id.y; y < M; y = y + 256u) {
      var sum = 0.0;
      for (var k = 0u; k < K; k = k + 1u) {
        sum = sum + array_a[y * strideA0 + k * strideA1] * array_b[k * strideB0 + x * strideB1];
      }
      array_y[x + y * N] = sum * alpha;
    }
  }
}
`,
  "gemm_c": `@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read> array_b: array<f32>;
@group(0) @binding(2) var<storage, read> array_c: array<f32>;
@group(0) @binding(3) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  M: u32,
  N: u32,
  K: u32,
  strideA0: u32,
  strideA1: u32,
  strideB0: u32,
  strideB1: u32,
  strideC0: u32,
  strideC1: u32,
  alpha: f32,
  beta: f32,
}

@group(0) @binding(4) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(8, 8, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let M = metaBuffer.M;
  let N = metaBuffer.N;
  let K = metaBuffer.K;
  let strideA0 = metaBuffer.strideA0;
  let strideA1 = metaBuffer.strideA1;
  let strideB0 = metaBuffer.strideB0;
  let strideB1 = metaBuffer.strideB1;
  let strideC0 = metaBuffer.strideC0;
  let strideC1 = metaBuffer.strideC1;
  let alpha = metaBuffer.alpha;
  let beta = metaBuffer.beta;
  for (var x = global_id.x; x < N; x = x + 256u) {
    for (var y = global_id.y; y < M; y = y + 256u) {
      var sum = 0.0;
      for (var k = 0u; k < K; k = k + 1u) {
        sum = sum + array_a[y * strideA0 + k * strideA1] * array_b[k * strideB0 + x * strideB1];
      }
      array_y[x + y * N] = sum * alpha + array_c[y * strideC0 + x * strideC1] * beta;
    }
  }
}
`,
  "mse_loss": `@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read> array_b: array<f32>;
@group(0) @binding(2) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  len: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(1, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  if (global_id.x != 0u) {
    return;
  }
  var v = 0.0;
  for (var i = 0u; i < len; i = i + 1u) {
    let diff = array_a[i] - array_b[i];
    v = v + diff * diff;
  }
  v = v / f32(len);
  array_y[0] = v;
}
`,
  "mse_loss_backprop": `@group(0) @binding(0) var<storage, read> array_a: array<f32>;
@group(0) @binding(1) var<storage, read> array_b: array<f32>;
@group(0) @binding(2) var<storage, read> array_gy: array<f32>;
@group(0) @binding(3) var<storage, read_write> array_ga: array<f32>;
@group(0) @binding(4) var<storage, read_write> array_gb: array<f32>;

struct MetaBuffer {
  len: u32,
  coef: f32,
}

@group(0) @binding(5) var<storage, read> metaBuffer: MetaBuffer;

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let coef = metaBuffer.coef;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let v = (array_a[i] - array_b[i]) * array_gy[0] * coef;
    array_ga[i] = v;
    array_gb[i] = -v;
  }
}
`,
  "nll_loss": `@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read> array_label: array<i32>;
@group(0) @binding(2) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  shape0: u32,
  shape1: u32,
}

@group(0) @binding(3) var<storage, read> metaBuffer: MetaBuffer;

fn get_array_x(dim0: u32, dim1: u32) -> f32 {
  return array_x[dim0 * metaBuffer.shape1 + dim1];
}

fn get_array_label(dim0: u32) -> i32 {
  return array_label[dim0];
}

@compute @workgroup_size(1, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  if (global_id.x != 0u) {
    return;
  }
  let shape0 = metaBuffer.shape0;
  var v = 0.0;
  for (var i = 0u; i < shape0; i = i + 1u) {
    v = v + log(get_array_x(i, u32(get_array_label(i))));
  }
  v = v / -f32(shape0);
  array_y[0] = v;
}
`,
  "softmax": `@group(0) @binding(0) var<storage, read> array_x: array<f32>;
@group(0) @binding(1) var<storage, read_write> array_y: array<f32>;

struct MetaBuffer {
  shape0: u32,
  shape1: u32,
}

@group(0) @binding(2) var<storage, read> metaBuffer: MetaBuffer;

fn get_array_x(dim0: u32, dim1: u32) -> f32 {
  return array_x[dim0 * metaBuffer.shape1 + dim1];
}

fn get_array_y(dim0: u32, dim1: u32) -> f32 {
  return array_y[dim0 * metaBuffer.shape1 + dim1];
}

fn set_array_y(val: f32, dim0: u32, dim1: u32) {
  array_y[dim0 * metaBuffer.shape1 + dim1] = val;
}

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let shape0 = metaBuffer.shape0;
  let shape1 = metaBuffer.shape1;
  for (var i = global_id.x; i < shape0; i = i + 4096u) {
    var top = 0.0;
    for (var j = 0u; j < shape1; j = j + 1u) {
      let v = get_array_x(i, j);
      if (v > top) {
        top = v;
      }
    }
    var expsum = 0.0;
    for (var j = 0u; j < shape1; j = j + 1u) {
      let v = get_array_x(i, j);
      let e = exp(v - top);
      set_array_y(e, i, j);
      expsum = expsum + e;
    }
    let inv_expsum = 1.0 / expsum;
    for (var j = 0u; j < shape1; j = j + 1u) {
      let v = get_array_y(i, j);
      set_array_y(v * inv_expsum, i, j);
    }
  }
}
`,
  "softmax_cross_entropy_backward": `@group(0) @binding(0) var<storage, read> array_softmax: array<f32>;
@group(0) @binding(1) var<storage, read> array_label: array<i32>;
@group(0) @binding(2) var<storage, read> array_gy: array<f32>;
@group(0) @binding(3) var<storage, read_write> array_gx: array<f32>;

struct MetaBuffer {
  len: u32,
  shape0: u32,
  shape1: u32,
}

@group(0) @binding(4) var<storage, read> metaBuffer: MetaBuffer;

fn get_array_softmax(dim0: u32, dim1: u32) -> f32 {
  return array_softmax[dim0 * metaBuffer.shape1 + dim1];
}

fn get_array_label(dim0: u32) -> i32 {
  return array_label[dim0];
}

fn get_array_gy(dim0: u32, dim1: u32) -> f32 {
  return array_gy[dim0 * metaBuffer.shape1 + dim1];
}

fn set_array_gx(val: f32, dim0: u32, dim1: u32) {
  array_gx[dim0 * metaBuffer.shape1 + dim1] = val;
}

@compute @workgroup_size(64, 1, 1)
fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {
  let len = metaBuffer.len;
  let shape0 = metaBuffer.shape0;
  let shape1 = metaBuffer.shape1;
  for (var i = global_id.x; i < len; i = i + 4096u) {
    let dim1 = i % shape1;
    let dim0 = i / shape1;

    var v = get_array_softmax(dim0, dim1);
    let labelValue = get_array_label(dim0);
    if (u32(labelValue) == dim1) {
      v = v - 1.0;
    }
    v = v * get_array_gy(dim0, dim1) / f32(shape0);
    set_array_gx(v, dim0, dim1);
  }
}
`,
};
